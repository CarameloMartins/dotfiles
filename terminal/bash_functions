#-------------------------------------------------------------------------------
# get_branch_name()
# This function returns branch name in current $PWD.
#-------------------------------------------------------------------------------

get_branch_name()
{
  git branch 2>/dev/null | sed -n -e 's/^\* \(.*\)/\1/p'
}
