# Add ~/.bin to $PATH.
export PATH="$PATH:$HOME/.local/bin:$HOME/.bin"

# Load prompt configurations.
source $HOME/".bash_prompt"

# Get external IP address.
alias exip="$HOME/.bin/exip.sh"

# Ping for 4 times.
alias ping="ping -c 4"
