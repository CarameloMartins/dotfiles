# ~/.bashrc

##
# Template .bashrc file settings.
# Source: http://tldp.org/LDP/abs/html/sample-bashrc.html
##

# If not running interactively, don't do anything
[ -z "$PS1" ] && return

##
# Source my own .bash_profile file.
##

if [ -f ~/.bash_profile ]; then
  source ~/.bash_profile
fi

# added by travis gem
[ -f /home/caramelomartins/.travis/travis.sh ] && source /home/caramelomartins/.travis/travis.sh
